language: php

php:
  - '7.2'

before_script:
  - cp .env.travis .env
  - touch database/database.sqlite
  - composer install --no-interaction
  - php artisan key:generate
  - php artisan migrate

script:
  - vendor/bin/phpunit